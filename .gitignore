__pycache__/
*.pyc

# --- Local-only: real student data must never commit ---
# Teacher roster (real names live here after adding via Roster UI)
config/students.json
# Google OAuth client secret + tokens (never share, never commit)
config/client_secret.json
config/token.json
# Homework runs: PDFs, reports, missing lists, markings (local only)
homework/*
!homework/.gitkeep
