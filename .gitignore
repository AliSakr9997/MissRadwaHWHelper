__pycache__/
*.pyc

# --- Local-only: real teacher/student data must never commit ---
# Per-teacher profiles: rosters, tokens, homework runs (all local only)
data/
# Legacy paths (kept ignored for old checkouts)
config/students.json
config/token.json
homework/*
!homework/.gitkeep
# Google OAuth client secret + tokens (never share, never commit)
config/client_secret.json
config/token.json
